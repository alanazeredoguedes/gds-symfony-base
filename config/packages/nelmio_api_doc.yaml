nelmio_api_doc:
    documentation:
        info:
            title: Documentação API
            description: Documentação das rotas API!
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []
    areas: # Agrupa a documentação da api em areas separadas
        default:
            path_patterns: [ ^/api ]


